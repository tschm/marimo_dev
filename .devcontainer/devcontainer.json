{
    "name": "Marimo Development Container",
    "dockerFile": "Dockerfile",
    "hostRequirements": {
        "cpus": 4
    },

    "remoteUser": "marimo",
    "updateRemoteUserUID": true,

    "forwardPorts": [8080],
    "postCreateCommand": "cd /workspaces && whoami && ls -all && uv pip install -r marimo_dev/requirements.txt",
    "postStartCommand": "marimo --yes edit /workspaces/marimo_dev --host=localhost --port=8080 --headless --no-token",
    "customizations": {
        "vscode": {
            "settings": {
                "python.defaultInterpreterPath": "/workspaces/marimo_dev/.venv/bin/python",
                "python.linting.enabled": true,
                "python.linting.pylintEnabled": true,
            },
            "extensions": [
                "ms-python.python",
                "marimo-team.vscode-marimo"
            ]
        }
    }
}
